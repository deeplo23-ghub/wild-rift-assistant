/**
 * Scoring engine internal types.
 *
 * Re-exports scoring types and defines engine-internal types.
 */

export type {
  ScoreBreakdown,
  WeightConfig,
  ScoredChampion,
  StageWeights,
  CompositionAnalysis,
  ThreatProfile,
} from "@/types/scoring";

export { ScoreComponent, CompositionElement, ThreatCategory } from "@/types/scoring";
